@model IEnumerable<School_D.Models.Grade>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Year)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSchoolNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSchoolNavigation.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdGrade">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdGrade">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdGrade">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
